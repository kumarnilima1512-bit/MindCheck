services:

  mindcheck:

    build:

      context: .

      dockerfile: Dockerfile

    container_name: mindcheck_app

    env_file:

      - .env

    ports:

      - '3000:3000'
      
    restart: on-failure:3